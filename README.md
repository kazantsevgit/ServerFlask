# ServerFlask

Простой Flask сервер для обработки JSON данных.
Система управления доступом к ключам и помещениям, построенная на Flask с использованием SQLAlchemy и Flask-Admin.
Выполнял роль тестового сервера для отладки и валидации функционала умной ключницы на ESP-32 Графит.

## ✨ Возможности

*   **Управление пользователями:** CRUD операции с пользователями через админ-панель
*   **Выдача ключей:** API endpoint для выдачи ключей пользователям с проверкой прав доступа
*   **Возврат ключей:** API endpoint для приема возвращенных ключей
*   **Валидация данных:** Проверка входных данных и обработка ошибок
*   **База данных SQLite:** Локальное хранение данных с миграциями через Flask-Migrate

## 🛠️ Технологии

*   **Backend:** Flask 2.3.2
*   **База данных:** SQLAlchemy + SQLite
*   **Админка:** Flask-Admin
*   **Миграции:** Flask-Migrate
*   **Язык:** Python 3.7+

## 📦 Установка и настройка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/kazantsevgit/ServerFlask.git
cd ServerFlask
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Инициализируйте базу данных:**
```bash
flask db init
flask db migrate -m "Initial migration"
flask db upgrade
```

## 🚀 Запуск приложения

**Разработческий режим:**
```bash
flask run
```
Приложение будет доступно по адресу `http://127.0.0.1:5000`

**Админ-панель:**
Админ-панель доступна по адресу `http://127.0.0.1:5000/admin`

## 📡 API Endpoints

### Выдача ключа
**POST** `/issue_key`
```json
{
    "serial": "12345ABC",
    "key": "101"
}
```

**Ответы:**
- `200` - Ключ успешно выдан
- `400` - Неверный запрос или ключ уже выдан
- `403` - Нет прав доступа к ключу
- `404` - Пользователь не найден
- `500` - Ошибка сервера

### Возврат ключа
**POST** `/return_key`
```json
{
    "serial": "12345ABC", 
    "key": "101"
}
```

**Ответы:**
- `200` - Ключ успешно возвращен
- `400` - Неверный запрос или ключ не был выдан
- `404` - Пользователь не найден
- `500` - Ошибка сервера

## 📁 Структура данных

### Модель User
```python
class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    fullname = db.Column(db.String(100), nullable=False)  # Полное имя
    serial = db.Column(db.String(50), unique=True, nullable=False)  # Серийный номер
    access = db.Column(JSON, nullable=False)  # Список доступных помещений
    taken_keys = db.Column(JSON, nullable=False, default=[])  # Взятые ключи
```

## 🔧 Администрирование

Админ-панель (`/admin`) позволяет:
- Просматривать список всех пользователей
- Добавлять/редактировать/удалять пользователей
- Настраивать права доступа к помещениям
- Просматривать выданные ключи
- Искать пользователей по имени и серийному номеру



